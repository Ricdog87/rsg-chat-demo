"use client"
// Helper component if needed in future; currently not used but kept for potential client-side schema injections.
// Intentionally left minimal to avoid runtime cost.
export default function OpenBreadcrumb() {
  return null
}
